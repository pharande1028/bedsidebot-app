[phases.setup]
nixPkgs = ['python39', 'libGL', 'glib', 'libxext', 'libSM', 'libxrender']

[phases.install]
cmds = ['pip install -r requirements.txt']

[phases.build]
cmds = ['echo "Build complete"']

[start]
cmd = 'python app.py'